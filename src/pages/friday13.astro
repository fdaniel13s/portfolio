---
---
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <title>Viernes 13 ✨💖</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
        body {
            background: radial-gradient(ellipse at center, #000 0%, #111 100%);
            color: #fff;
            font-family: 'Georgia', serif;
            min-height: 100vh;
            margin: 0;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 2rem;
        }

        h1 {
            font-size: 3rem;
            background: linear-gradient(45deg, #ff4e50, #f9d423);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: glow 2s infinite alternate;
            margin: 0;
        }

        p {
            font-size: 1.2rem;
            max-width: 600px;
            margin-top: 1rem;
            line-height: 1.6;
        }

        button {
            margin-top: 2rem;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            background: #ff4e50;
            color: #fff;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        button:hover {
            transform: scale(1.05);
            box-shadow: 0 0 20px #f9d423;
        }

        @keyframes glow {
            from {
                text-shadow: 0 0 10px #ff4e50, 0 0 20px #f9d423;
            }
            to {
                text-shadow: 0 0 20px #ff4e50, 0 0 40px #f9d423;
            }
        }

        .heart {
            position: absolute;
            width: 20px;
            height: 20px;
            background: red;
            transform: rotate(45deg);
            animation: floatUp 5s linear infinite;
        }

        .heart::before,
        .heart::after {
            content: '';
            position: absolute;
            width: 20px;
            height: 20px;
            background: red;
            border-radius: 50%;
        }

        .heart::before {
            top: -10px;
            left: 0;
        }

        .heart::after {
            left: -10px;
            top: 0;
        }

        @keyframes floatUp {
            0% {
                transform: translateY(0) rotate(45deg) scale(1);
                opacity: 1;
            }
            100% {
                transform: translateY(-800px) rotate(45deg) scale(0.5);
                opacity: 0;
            }
        }

        .sparkle {
            position: absolute;
            width: 5px;
            height: 5px;
            background: #fff;
            border-radius: 50%;
            animation: sparkle 3s infinite;
        }

        @keyframes sparkle {
            0% {
                opacity: 0;
                transform: scale(0.5);
            }
            50% {
                opacity: 1;
                transform: scale(1.5);
            }
            100% {
                opacity: 0;
                transform: scale(0.5);
            }
        }

        .modal {
            position: fixed;
            top: 0; left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.85);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.3s ease;
            padding: 1rem;
            box-sizing: border-box;
        }

        .modal.show {
            opacity: 1;
            pointer-events: auto;
        }

        .modal-content {
            background: #fff;
            color: #333;
            border-radius: 20px;
            padding: 2rem;
            max-width: 600px;
            width: 100%;
            text-align: left;
            font-family: 'Georgia', serif;
            position: relative;
            box-shadow: 0 0 30px #ff4e50;
        }

        .modal-content h2 {
            color: #ff4e50;
            margin-top: 0;
            font-size: 1.8rem;
        }

        .modal-content p {
            line-height: 1.6;
            font-size: 1.1rem;
        }

        .close {
            position: absolute;
            top: 10px; right: 20px;
            font-size: 2rem;
            color: #ff4e50;
            cursor: pointer;
        }

        .message:hover {
            transform: scale(1.02);
            transition: transform 0.3s ease;
        }

        /* Responsive tweaks */
        @media (max-width: 600px) {
            h1 {
                font-size: 2.2rem;
            }

            p {
                font-size: 1rem;
            }

            button {
                font-size: 1rem;
                padding: 0.8rem 1.5rem;
            }

            .modal-content {
                padding: 1.5rem;
            }

            .modal-content h2 {
                font-size: 1.5rem;
            }

            .modal-content p {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
<div class="message">
    <h1>Viernes 13 para ti Dianita ✨💖</h1>
    <p>
        Dicen que es de mala suerte... pero contigo, no hay nada de mala suerte. Desde que te
        conocí, cada día que se que cuento contigo es una bendición. Te quiero mucho Diana,
        no importa si es viernes 13 o cualquier otro día, siempre será especial porque estás tú.

    </p>
    <button id="openModal">Abrir mi carta para ti 💌</button>
</div>

<!-- corazones flotando -->
{Array.from({ length: 15 }).map(() => (
        <div
                class="heart"
                style={`left: ${Math.random() * 100}%; animation-delay: ${Math.random() * 5}s;`}
        ></div>
))}

<!-- destellos -->
{Array.from({ length: 20 }).map(() => (
        <div
                class="sparkle"
                style={`top: ${Math.random() * 100}%; left: ${Math.random() * 100}%; animation-delay: ${
                    Math.random() * 3
                }s;`}
        ></div>
))}

<!-- Modal -->
<div id="myModal" class="modal">
    <div class="modal-content">
        <span class="close" id="closeModal">&times;</span>
        <h2>Para ti, Diana Soto ✨</h2>
        <p>
            Como te decía, eres muy especial para mí: me encanta cada detalle tuyo,
            tu risa, tus ojos, tu carita cuando te enojas, tu forma de hablar, de pensar,
            de estudiar, de quererme, de cuidarme, de hacerme reír… y sobre todo la forma
            en la que me haces sentir la persona más feliz del mundo. 💫
            <br /><br />
            Gracias por ser parte de mi vida, y quiero que lo sigas siendo por muchísimo tiempo más.
            Ahora, cierra los ojos un momento, respira hondo... y escucha esta canción que te dedico
            con todo mi amor:
        </p>

        <div style="
          margin-top: 1.5rem;
          text-align: center;
        ">
                    <a
                            href="https://youtu.be/ubq19HWOYo4"
                            target="_blank"
                            rel="noopener noreferrer"
                            style="
              display: inline-block;
              padding: 0.8rem 1.8rem;
              background: linear-gradient(45deg, #ff4e50, #f9d423);
              color: #fff;
              font-weight: bold;
              border-radius: 50px;
              text-decoration: none;
              font-size: 1.1rem;
              transition: transform 0.3s ease, box-shadow 0.3s ease;
            "
                            onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 0 15px #f9d423';"
                            onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='none';"
                    >
                        💖 Escuchar canción 💖
                    </a>
                </div>

                <p style="margin-top: 2rem;">
                    Con amor, <br />
                    <strong>Fernando Daniel ❤️</strong>
                </p>

    </div>
</div>

<script>
    const openModal = document.getElementById('openModal');
    const modal = document.getElementById('myModal');
    const closeModal = document.getElementById('closeModal');

    openModal.onclick = () => {
        modal.classList.add('show');
    };

    closeModal.onclick = () => {
        modal.classList.remove('show');
    };

    window.onclick = (e) => {
        if (e.target === modal) {
            modal.classList.remove('show');
        }
    };
</script>
</body>
</html>
